package com.github.otr.console_client.handler

/**
 * All the handlers should be registered as enum class values for convenience.
 */
enum class HandlerType {
    AUTH, COMMAND, COMMON, GET_ME, NEW_MESSAGE
}
